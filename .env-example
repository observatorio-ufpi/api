# Banco educação (porta 5438) — usado pelo import do dump
POSTGRES_EDUCACAO_DB=educacao-database
POSTGRES_EDUCACAO_USER=root
POSTGRES_EDUCACAO_PASSWORD=teste
DATABASE_URL_EDUCACAO="postgresql://root:teste@localhost:5438/educacao-database?schema=public"

# Banco fiscal / observatório (porta 5436) — não entra no dump
POSTGRES_DB=observatorio-database
POSTGRES_USER=root
POSTGRES_PASSWORD=teste
COMPOSE_PROJECT_NAME=observatorio
DATABASE_URL="postgresql://root:teste@localhost:5436/observatorio-database?schema=public"

PORT=3003
